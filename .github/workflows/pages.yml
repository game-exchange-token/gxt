name: Deploy Pages

on:
  push:
    tags:
      - v*
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install system dependencies (clang/llvm)
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick librsvg2-bin

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-bindgen-cli
        run: cargo install wasm-bindgen-cli

      - name: Build gxt-wasm
        run: |
          cd gxt-wasm
          cargo build --release --target wasm32-unknown-unknown

      - name: Generate web bindings
        run: |
          mkdir -p web/pkg
          wasm-bindgen --target web --no-typescript \
            --out-dir web/pkg \
            target/wasm32-unknown-unknown/release/gxt_wasm.wasm
          touch web/.nojekyll
          # Build favicon.ico from svg (multiple sizes)
          rsvg-convert -w 256 -h 256 web/favicon.svg -o /tmp/favicon-256.png
          rsvg-convert -w 128 -h 128 web/favicon.svg -o /tmp/favicon-128.png
          rsvg-convert -w 32 -h 32 web/favicon.svg -o /tmp/favicon-32.png
          convert /tmp/favicon-256.png /tmp/favicon-128.png /tmp/favicon-32.png web/favicon.ico

      - name: Build site structure (root = app, /web = README)
        run: |
          rm -rf site
          mkdir -p site
          # Copy SPA (current app lives in web/) to site root
          cp -R web/* site/
          # Generate web/index.html from README.md
          sudo apt-get update && sudo apt-get install -y pandoc
          mkdir -p site/web
          pandoc -f gfm -t html5 README.md -o site/web/index.html \
            --metadata title="GXT — Documentation" \
            -s
          # Ensure GitHub Pages doesn't run Jekyll on artifact
          touch site/.nojekyll

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
